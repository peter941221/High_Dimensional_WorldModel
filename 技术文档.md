
仓库地址： https://github.com/peter941221/High_Dimensional_WorldModel


# 模型选择：从头训练 vs 用现成模型？用哪个架构？

---

## 先回答核心问题：为什么必须从头训练

**不存在一个现成的"4D物理世界模型"可以直接用。**

原因很简单：

```
现有的预训练模型          你的项目
─────────────          ──────────
GPT-4/5 系列            输入是文字          你的输入是 4D 物理状态向量
Sora                   输入是视频像素       你的输入是坐标和速度数字
DreamerV3 预训练权重    训练在 Atari/DMC    你的环境是自定义 4D 物理
Stable Diffusion       图像生成            和你的任务毫无关系
```

**但"从头训练"并不可怕**，因为：

- 你的模型很小：几十万参数（不是几十亿）
- 你的数据无限：物理引擎随时可以生成
- 你的状态空间很小：20-50 维向量（不是百万像素）
- 训练时间很短：Colab T4 上几小时就能收敛

---

## "从头训练"≠"从零发明"

你不需要发明新架构。你做的是：

```
选一个已经被验证有效的架构设计（蓝图）
         ↓
用你的 4D 物理数据从头训练这个架构的参数（施工）
         ↓
得到一个专门理解 4D 物理的世界模型（成品）
```

就像盖房子：你不需要发明"钢筋混凝土结构"，你用现成的结构设计，但要自己浇筑。

---

## 五个候选架构，逐个分析

### 架构 1：纯前馈 MLP（最简单）

```
输入：[当前状态, 动作] → 拼在一起
  ↓
几层全连接层 (Linear → ReLU → Linear → ReLU → Linear)
  ↓
输出：预测的下一个状态
```

```python
class MLPWorldModel(nn.Module):
    def __init__(self, state_dim, action_dim, hidden=256):
        super().__init__()
        self.net = nn.Sequential(
            nn.Linear(state_dim + action_dim, hidden),
            nn.ReLU(),
            nn.Linear(hidden, hidden),
            nn.ReLU(),
            nn.Linear(hidden, state_dim)  # 预测下一个状态
        )
    
    def forward(self, state, action):
        x = torch.cat([state, action], dim=-1)
        return self.net(x)  # 预测 s_{t+1}
```

| 优点 | 缺点 |
|------|------|
| 代码 10 行就写完 | 没有记忆，每一步都是独立的 |
| 调试最容易 | 无法处理部分可观测问题 |
| 训练最快 | 长期预测（rollout 多步）误差会快速累积 |

**适合：第一个原型，验证整个流程跑通**

---

### 架构 2：GRU 循环模型（Ha & Schmidhuber 2018 的做法）

```
时间步 t:
  输入 [状态_t, 动作_t] → GRU → 隐状态 h_t → 预测状态_{t+1}

时间步 t+1:
  输入 [状态_{t+1}, 动作_{t+1}] + 隐状态 h_t → GRU → h_{t+1} → 预测状态_{t+2}

... 以此类推
```

```python
class GRUWorldModel(nn.Module):
    def __init__(self, state_dim, action_dim, hidden=256):
        super().__init__()
        self.encoder = nn.Linear(state_dim, hidden)
        self.gru = nn.GRUCell(hidden + action_dim, hidden)
        self.decoder = nn.Linear(hidden, state_dim)
        self.reward_head = nn.Linear(hidden, 1)
        self.hidden_dim = hidden
    
    def init_hidden(self, batch_size, device):
        return torch.zeros(batch_size, self.hidden_dim, device=device)
    
    def forward(self, state, action, hidden):
        z = F.relu(self.encoder(state))
        x = torch.cat([z, action], dim=-1)
        hidden = self.gru(x, hidden)
        next_state = self.decoder(hidden)
        reward = self.reward_head(hidden)
        return next_state, reward, hidden
```

| 优点 | 缺点 |
|------|------|
| 有记忆（GRU 隐状态） | 隐状态是确定性的，无法表达不确定性 |
| 多步 rollout 更稳定 | 比 RSSM 简单但也弱一些 |
| 经典验证过的架构 | |
| 代码量适中 | |

**适合：正式实验的主力架构**

---

### 架构 3：RSSM（Dreamer 系列用的，最标准）

```
RSSM = Recurrent State Space Model

状态分两部分：
  h_t = 确定性隐状态（GRU 维护，像"记忆"）
  z_t = 随机性潜变量（采样得到，像"感知的不确定性"）

完整状态 = [h_t, z_t]

转移过程：
  h_t = GRU(h_{t-1}, z_{t-1}, a_{t-1})     ← 确定性部分
  z_t ~ q(z_t | h_t, o_t)                   ← 用真实观测修正（后验）
  ẑ_t ~ p(z_t | h_t)                        ← 纯想象时用这个（先验）
```

```
为什么要两部分？

想象你在开车：
  确定性部分 h：你知道车在高速上以 100km/h 行驶（确定的记忆）
  随机性部分 z：前方可能有车，也可能没有（不确定的感知）

世界模型需要同时建模"确定会发生的事"和"可能发生的事"
```

```python
class RSSM(nn.Module):
    def __init__(self, state_dim, action_dim, 
                 det_dim=200, stoch_dim=30, hidden=200):
        super().__init__()
        
        # 确定性路径
        self.gru = nn.GRUCell(stoch_dim + action_dim, det_dim)
        
        # 先验（想象时用）：只看 h_t，猜 z_t
        self.prior_net = nn.Sequential(
            nn.Linear(det_dim, hidden), nn.ReLU(),
            nn.Linear(hidden, stoch_dim * 2)  # 输出均值和方差
        )
        
        # 后验（有真实观测时用）：看 h_t 和 o_t，推断 z_t
        self.posterior_net = nn.Sequential(
            nn.Linear(det_dim + state_dim, hidden), nn.ReLU(),
            nn.Linear(hidden, stoch_dim * 2)
        )
        
        # 解码器
        self.decoder = nn.Sequential(
            nn.Linear(det_dim + stoch_dim, hidden), nn.ReLU(),
            nn.Linear(hidden, state_dim)
        )
        
        self.reward_head = nn.Sequential(
            nn.Linear(det_dim + stoch_dim, hidden), nn.ReLU(),
            nn.Linear(hidden, 1)
        )
        
        self.det_dim = det_dim
        self.stoch_dim = stoch_dim
    
    def forward_prior(self, h):
        """先验：想象时，只根据记忆猜测当前感知"""
        params = self.prior_net(h)
        mean, log_std = params.chunk(2, dim=-1)
        std = F.softplus(log_std) + 0.1
        z = mean + std * torch.randn_like(std)  # 重参数化采样
        return z, mean, std
    
    def forward_posterior(self, h, obs):
        """后验：有真实观测时，修正感知"""
        params = self.posterior_net(torch.cat([h, obs], dim=-1))
        mean, log_std = params.chunk(2, dim=-1)
        std = F.softplus(log_std) + 0.1
        z = mean + std * torch.randn_like(std)
        return z, mean, std
    
    def step(self, prev_h, prev_z, action):
        """确定性路径：更新记忆"""
        x = torch.cat([prev_z, action], dim=-1)
        h = self.gru(x, prev_h)
        return h
    
    def decode(self, h, z):
        """解码：从完整状态预测观测"""
        full_state = torch.cat([h, z], dim=-1)
        obs = self.decoder(full_state)
        reward = self.reward_head(full_state)
        return obs, reward
```

| 优点 | 缺点 |
|------|------|
| 能建模不确定性 | 代码复杂度最高 |
| Dreamer 系列验证过，效果最好 | 需要理解 VAE 和 KL 散度 |
| 想象/做梦的质量最高 | 调参更多 |
| 论文发表认可度高（用了标准方法） | |

**适合：正式研究和论文**

---

### 架构选择的最终建议

```
你的实施路线：

第 1 步（第 1-2 周）：用 MLP 架构
  ├── 目的：验证物理引擎 + 训练流程跑通
  ├── 复杂度：低
  └── 产出：确认整个 pipeline 无 bug

第 2 步（第 3-5 周）：切换到 GRU 架构  ← 主力架构
  ├── 目的：正式实验，有记忆能力
  ├── 复杂度：中
  └── 产出：维度对比实验、迁移实验的主要结果

第 3 步（第 6-7 周，可选）：升级到 RSSM
  ├── 目的：对比"确定性 vs 随机性"世界模型
  ├── 复杂度：高
  └── 产出：消融实验（ablation），论文的附加实验
```

**一句话：先 MLP 验证流程 → GRU 跑主实验 → 有余力再上 RSSM。**

---

---

# 完整技术文档

## 项目名称
**HyperDream: Higher-Dimensional World Models for Cross-Dimensional Transfer**
**（超维做梦：高维世界模型的跨维度迁移研究）**

---

## 1. 项目概述

### 1.1 研究问题
> 在更高维度（4D、5D …）的物理世界中训练的世界模型，能否学到更一般化的物理直觉，从而在降维到 3D 任务时获得更好的表现？

### 1.2 核心假设
- **假设 H1**：4D 预训练的世界模型迁移到 3D 后，学习速度快于 3D 从头训练
- **假设 H2**：存在一个最优预训练维度 $d^*$，使得迁移到 3D 的效果最佳
- **假设 H3**：高维预训练的世界模型对 3D 中的扰动更鲁棒

### 1.3 技术路线总图

```
┌──────────────────────────────────────────────────────────────┐
│                    HyperDream 系统架构                        │
│                                                              │
│  ┌─────────────┐    ┌──────────────┐    ┌────────────────┐  │
│  │  N维物理引擎 │───→│  N维RL环境   │───→│   经验缓冲区   │  │
│  │ (2D~8D可配) │    │ (Gymnasium)  │    │ (ReplayBuffer) │  │
│  └─────────────┘    └──────────────┘    └───────┬────────┘  │
│                                                  │           │
│                              ┌───────────────────┘           │
│                              ↓                               │
│                    ┌──────────────────┐                      │
│                    │    世界模型训练    │                      │
│                    │  (MLP→GRU→RSSM)  │                      │
│                    └────────┬─────────┘                      │
│                             │                                │
│                    ┌────────↓─────────┐                      │
│                    │   梦境策略训练     │                      │
│                    │  (Imagination)    │                      │
│                    └────────┬─────────┘                      │
│                             │                                │
│               ┌─────────────┴─────────────┐                  │
│               ↓                           ↓                  │
│    ┌─────────────────┐         ┌─────────────────┐          │
│    │ 维度对比实验      │         │  跨维度迁移实验   │          │
│    │ 2D vs 3D vs 4D  │         │  4D→3D transfer  │          │
│    └─────────────────┘         └─────────────────┘          │
│               │                           │                  │
│               └─────────────┬─────────────┘                  │
│                             ↓                                │
│                    ┌──────────────────┐                      │
│                    │   评估与分析      │                      │
│                    │  指标/可视化/报告 │                      │
│                    └──────────────────┘                      │
└──────────────────────────────────────────────────────────────┘
```

---

## 2. 模块详细设计

### 2.1 模块 A：N 维物理引擎（`physics/engine.py`）

#### 功能描述
可配置维度的刚体物理模拟器，支持 2D 到 8D。

#### 接口定义

```python
class Body4D:
    """N 维刚体"""
    position: torch.Tensor   # shape (dim,)
    velocity: torch.Tensor   # shape (dim,)
    mass: float
    radius: float
    
class Physics4D:
    """N 维物理引擎"""
    def __init__(self, dim: int, gravity_axis: int, 
                 gravity_strength: float, dt: float, boundary: float)
    
    def step(self, bodies: List[Body4D], 
             external_forces: List[torch.Tensor]) -> None
        """推进一个时间步"""
    
    def get_state(self, bodies: List[Body4D]) -> torch.Tensor
        """返回所有物体的状态向量"""
    
    def get_energy(self, bodies: List[Body4D]) -> dict
        """返回动能+势能（用于验证能量守恒）"""
```

#### 验收标准 A

| 编号 | 测试项 | 通过标准 | 验证方法 |
|------|--------|---------|---------|
| A1 | 维度可配置 | dim=2,3,4,5,6,8 都能正常运行 | 自动化单元测试 |
| A2 | 动量守恒 | 无外力碰撞后总动量误差 < 1e-5 | `test_momentum_conservation()` |
| A3 | 能量守恒（弹性碰撞） | 碰撞前后总能量误差 < 1e-4 | `test_energy_conservation()` |
| A4 | 自由落体精度 | 与解析解对比，位置误差 < 1% | `test_free_fall()` |
| A5 | 边界反弹正确 | 物体不穿越边界 | `test_boundary()` |
| A6 | 性能基准 | 10 个物体 × 10000 步 < 5 秒（CPU） | `benchmark()` |
| A7 | 确定性 | 相同初始条件，两次运行结果完全一致 | `test_determinism()` |

#### 验证代码示例

```python
def test_momentum_conservation():
    """A2: 动量守恒测试"""
    for dim in [2, 3, 4, 5]:
        physics = Physics4D(dim=dim, gravity_strength=0)  # 无重力
        
        bodies = [
            Body4D(position=torch.randn(dim), 
                   velocity=torch.randn(dim), mass=1.0+i*0.5)
            for i in range(5)
        ]
        
        # 碰撞前总动量
        p_before = sum(b.mass * b.velocity for b in bodies)
        
        for _ in range(5000):
            physics.step(bodies)
        
        # 碰撞后总动量
        p_after = sum(b.mass * b.velocity for b in bodies)
        
        error = torch.norm(p_before - p_after).item()
        assert error < 1e-5, f"{dim}D 动量守恒误差过大: {error}"
        print(f"  {dim}D 动量守恒 ✓ (误差: {error:.2e})")


def test_free_fall():
    """A4: 自由落体解析解对比"""
    for dim in [2, 3, 4, 5]:
        g = -9.8
        dt = 0.001  # 小步长提高精度
        physics = Physics4D(dim=dim, gravity_axis=1, 
                           gravity_strength=g, dt=dt, boundary=1000)
        
        body = Body4D(
            position=torch.zeros(dim),
            velocity=torch.zeros(dim),
            mass=1.0, radius=0.1
        )
        
        T = 2.0  # 模拟 2 秒
        steps = int(T / dt)
        
        for _ in range(steps):
            physics.step([body])
        
        # 解析解：y = 0.5 * g * t^2
        expected_y = 0.5 * g * T**2
        actual_y = body.position[1].item()
        
        relative_error = abs(actual_y - expected_y) / abs(expected_y)
        assert relative_error < 0.01, f"{dim}D 自由落体误差: {relative_error:.4%}"
        print(f"  {dim}D 自由落体 ✓ (相对误差: {relative_error:.4%})")
```

---

### 2.2 模块 B：RL 环境（`envs/push_ball.py`）

#### 功能描述
将物理引擎封装为标准 RL 环境接口。

#### 任务定义

```
任务：推球到目标点

初始状态：
  - 智能体在原点附近随机位置
  - 球在随机位置
  - 目标点在随机位置

动作空间：
  - N 维连续力向量，每个分量 ∈ [-1, 1]

观测空间：
  - 智能体位置 (dim)
  - 智能体速度 (dim)
  - 球位置 (dim)
  - 球速度 (dim)
  - 目标位置 (dim)
  - 总计：5 × dim 维

奖励设计：
  - 每步：-||球位置 - 目标位置||  （球越近目标越好）
  - 成功奖励：+100（球到达目标附近 0.5 以内）
  - 能量惩罚：-0.01 × ||动作||^2 （鼓励用小力）

终止条件：
  - 成功（球到目标 < 0.5）
  - 超时（超过 max_steps = 200）
```

#### 接口定义

```python
class PushBallNDEnv:
    def __init__(self, dim: int = 4, max_steps: int = 200, 
                 difficulty: str = 'easy')
    
    def reset(self, seed: int = None) -> torch.Tensor
        """返回初始状态向量"""
    
    def step(self, action: torch.Tensor) -> Tuple[torch.Tensor, float, bool, dict]
        """返回 (next_state, reward, done, info)"""
    
    @property
    def state_dim(self) -> int
    
    @property
    def action_dim(self) -> int
```

#### 难度级别

```python
difficulty_configs = {
    'easy': {
        'init_range': 2.0,      # 初始位置范围小
        'target_range': 3.0,    # 目标位置范围小
        'num_obstacles': 0,     # 无障碍物
        'gravity': True,        # 有重力
    },
    'medium': {
        'init_range': 4.0,
        'target_range': 5.0,
        'num_obstacles': 2,     # 2 个障碍物
        'gravity': True,
    },
    'hard': {
        'init_range': 6.0,
        'target_range': 8.0,
        'num_obstacles': 5,     # 5 个障碍物
        'gravity': True,
        'wind': True,           # 随机风力干扰
    },
}
```

#### 验收标准 B

| 编号 | 测试项 | 通过标准 | 验证方法 |
|------|--------|---------|---------|
| B1 | 维度切换 | dim=2~8 均能 reset 和 step | 循环测试 |
| B2 | 状态维度正确 | `state.shape == (5 * dim,)` | 断言检查 |
| B3 | 奖励范围合理 | 单步奖励 ∈ [-50, 100] | 1000 步随机动作统计 |
| B4 | 随机策略成功率 | easy 难度下随机策略成功率 < 5% | 1000 局统计（说明任务不平凡） |
| B5 | 最优策略可达 | 手写启发式策略成功率 > 50% | 验证任务可解 |
| B6 | 环境可复现 | 相同 seed 产生完全相同轨迹 | `test_reproducibility()` |
| B7 | 每步耗时 | 单步 < 1ms | 计时测试 |

#### 验证代码示例

```python
def test_env_basics():
    """B1-B3: 基础功能测试"""
    for dim in [2, 3, 4, 5, 6, 8]:
        env = PushBallNDEnv(dim=dim)
        
        # B1: 能正常 reset
        state = env.reset(seed=42)
        assert state is not None, f"{dim}D reset 失败"
        
        # B2: 状态维度正确
        assert state.shape == (5 * dim,), \
            f"{dim}D 状态维度错误: 期望 {5*dim}, 得到 {state.shape}"
        
        # B3: 奖励范围
        rewards = []
        state = env.reset()
        for _ in range(1000):
            action = torch.randn(dim).clamp(-1, 1)
            state, reward, done, info = env.step(action)
            rewards.append(reward)
            if done:
                state = env.reset()
        
        min_r, max_r = min(rewards), max(rewards)
        assert min_r > -100, f"{dim}D 奖励下界异常: {min_r}"
        assert max_r < 200, f"{dim}D 奖励上界异常: {max_r}"
        
        print(f"  {dim}D 环境基础测试 ✓ "
              f"(奖励范围: [{min_r:.1f}, {max_r:.1f}])")


def test_random_vs_heuristic():
    """B4-B5: 验证任务难度合理"""
    dim = 4
    env = PushBallNDEnv(dim=dim, difficulty='easy')
    
    # 随机策略
    random_successes = 0
    for ep in range(1000):
        state = env.reset()
        done = False
        while not done:
            action = torch.randn(dim).clamp(-1, 1)
            state, _, done, info = env.step(action)
        if info['success']:
            random_successes += 1
    
    random_rate = random_successes / 1000
    assert random_rate < 0.05, f"随机策略成功率过高: {random_rate:.1%}"
    
    # 启发式策略（朝球走，球朝目标推）
    heuristic_successes = 0
    for ep in range(1000):
        state = env.reset()
        done = False
        while not done:
            # 解析状态
            agent_pos = state[:dim]
            ball_pos = state[2*dim:3*dim]
            target_pos = state[4*dim:5*dim]
            
            # 简单策略：朝"球到目标"方向的反面走，推球
            ball_to_target = target_pos - ball_pos
            direction = ball_to_target / (torch.norm(ball_to_target) + 1e-8)
            push_point = ball_pos - direction * 0.8  # 走到球的背后
            
            agent_to_push = push_point - agent_pos
            action = agent_to_push / (torch.norm(agent_to_push) + 1e-8)
            
            state, _, done, info = env.step(action)
        if info['success']:
            heuristic_successes += 1
    
    heuristic_rate = heuristic_successes / 1000
    assert heuristic_rate > 0.30, f"启发式策略成功率过低: {heuristic_rate:.1%}"
    
    print(f"  随机策略成功率: {random_rate:.1%} (应 < 5%)")
    print(f"  启发式策略成功率: {heuristic_rate:.1%} (应 > 30%)")
    print(f"  任务难度合理 ✓")
```

---

### 2.3 模块 C：世界模型（`models/world_model.py`）

#### 三级架构渐进实现

```
Level 1: MLPWorldModel     ← 第 1-2 周验证用
Level 2: GRUWorldModel     ← 第 3-5 周主力实验
Level 3: RSSMWorldModel    ← 第 6-7 周进阶实验
```

#### 接口定义（三个级别共享同一接口）

```python
class BaseWorldModel(nn.Module):
    """所有世界模型的统一接口"""
    
    def encode(self, state: Tensor) -> Tensor:
        """状态 → 潜空间"""
    
    def predict_next(self, hidden: Tensor, action: Tensor) -> Tuple:
        """预测下一步：返回 (new_hidden, pred_state, pred_reward)"""
    
    def imagine(self, start_state: Tensor, 
                action_seq: Tensor) -> Dict:
        """做梦：返回 {'states': ..., 'rewards': ...}"""
    
    def compute_loss(self, batch: Dict) -> Dict:
        """计算训练损失：返回 {'total': ..., 'state': ..., 'reward': ..., 'kl': ...}"""
```

#### 超参数配置

```python
model_configs = {
    'mlp': {
        'hidden_dim': 256,
        'num_layers': 3,
        'activation': 'relu',
        # 参数量 ≈ 200K
    },
    'gru': {
        'hidden_dim': 256,
        'encoder_layers': 2,
        'decoder_layers': 2,
        # 参数量 ≈ 500K
    },
    'rssm': {
        'det_dim': 200,         # 确定性隐状态维度
        'stoch_dim': 30,        # 随机性潜变量维度
        'hidden_dim': 200,
        'kl_weight': 1.0,       # KL 散度损失权重
        'free_nats': 3.0,       # KL 自由比特（Dreamer 的技巧）
        # 参数量 ≈ 800K
    },
}
```

#### 验收标准 C

| 编号 | 测试项 | 通过标准 | 验证方法 |
|------|--------|---------|---------|
| C1 | 前向传播无报错 | 三个级别模型，dim=2~8，batch=1和64 | `test_forward()` |
| C2 | 梯度流通 | `loss.backward()` 后所有参数有梯度 | `test_gradients()` |
| C3 | 单步预测精度 | 训练 1000 步后，状态预测 MSE < 0.1 | `test_single_step()` |
| C4 | 多步预测衰减 | 15 步 rollout 后误差 < 5×单步误差 | `test_rollout_error()` |
| C5 | 奖励预测精度 | 奖励预测 MSE < 1.0 | `test_reward_prediction()` |
| C6 | 想象轨迹合理 | 想象中的物体不穿墙、不瞬移 | `test_imagination_physics()` |
| C7 | 过拟合测试 | 在 100 条轨迹上能过拟合到 MSE < 0.01 | 验证模型容量足够 |

#### 核心验证代码

```python
def test_rollout_error():
    """C4: 多步预测误差是否可控"""
    dim = 4
    env = PushBallNDEnv(dim=dim)
    model = GRUWorldModel(state_dim=5*dim, action_dim=dim).to(device)
    
    # 先训练世界模型 2000 步（用随机数据）
    train_world_model(model, env, num_steps=2000)
    
    # 测试：真实轨迹 vs 想象轨迹
    state = env.reset()
    hidden = model.init_hidden(1, device)
    
    real_states = [state]
    pred_states = [state]
    
    for t in range(15):
        action = torch.randn(dim).clamp(-1, 1)
        
        # 真实环境
        next_real, _, _, _ = env.step(action)
        real_states.append(next_real)
        
        # 世界模型预测
        with torch.no_grad():
            s = pred_states[-1].unsqueeze(0).to(device)
            a = action.unsqueeze(0).to(device)
            pred_next, _, hidden = model(s, a, hidden)
            pred_states.append(pred_next.squeeze(0).cpu())
    
    # 计算每步的累积误差
    errors = [torch.norm(real_states[t] - pred_states[t]).item() 
              for t in range(16)]
    
    single_step_error = errors[1]
    final_error = errors[15]
    error_ratio = final_error / (single_step_error + 1e-8)
    
    print(f"  单步误差: {single_step_error:.4f}")
    print(f"  15步误差: {final_error:.4f}")
    print(f"  误差放大倍数: {error_ratio:.1f}x")
    
    assert error_ratio < 5.0, f"误差放大过快: {error_ratio:.1f}x"
    print(f"  多步预测稳定性 ✓")
```

---

### 2.4 模块 D：梦境训练（`training/dream_trainer.py`）

#### 训练流程伪代码

```
for epoch in range(num_epochs):
    
    ┌─ 阶段 1: 收集真实经验 ──────────────────────────┐
    │  for episode in range(collect_episodes):          │
    │      运行策略在真实环境中，收集 (s, a, r, s') 存入 buffer │
    └──────────────────────────────────────────────────┘
                            ↓
    ┌─ 阶段 2: 训练世界模型 ──────────────────────────┐
    │  for step in range(wm_train_steps):               │
    │      从 buffer 采样序列                            │
    │      用世界模型预测，计算预测误差                    │
    │      反向传播，更新世界模型参数                      │
    └──────────────────────────────────────────────────┘
                            ↓
    ┌─ 阶段 3: 在梦中训练策略 ────────────────────────┐
    │  for step in range(dream_train_steps):            │
    │      从 buffer 采样起始状态                        │
    │      用策略 + 世界模型展开 H 步想象轨迹            │
    │      计算想象中的累积奖励                           │
    │      反向传播到策略，最大化想象奖励                  │
    └──────────────────────────────────────────────────┘
                            ↓
    记录指标、保存检查点
```

#### 验收标准 D

| 编号 | 测试项 | 通过标准 | 验证方法 |
|------|--------|---------|---------|
| D1 | 训练不崩溃 | 50 个 epoch 无 NaN、无 OOM | 完整训练运行 |
| D2 | 世界模型损失下降 | 最终损失 < 初始损失的 20% | 损失曲线 |
| D3 | 策略性能提升 | 最终奖励 > 初始奖励 + 50% | 奖励曲线 |
| D4 | 梦中训练有效 | "有做梦"的性能 > "无做梦"的性能 | 对比实验 |
| D5 | 检查点可恢复 | 保存后加载，继续训练无异常 | `test_checkpoint()` |
| D6 | 训练时间可控 | 50 epoch 在 Colab T4 上 < 2 小时 | 计时 |
| D7 | 显存可控 | 峰值显存 < 4GB | `torch.cuda.max_memory_allocated()` |

---

### 2.5 模块 E：经验回放缓冲区（`training/buffer.py`）

```python
class ReplayBuffer:
    """
    存储真实经验的缓冲区
    支持按序列采样（用于训练循环模型）
    """
    
    def __init__(self, capacity: int = 100000):
        self.capacity = capacity
        self.episodes = []  # 每个 episode 是一条完整轨迹
    
    def add_episode(self, episode: Dict[str, torch.Tensor]):
        """
        添加一条轨迹
        episode = {
            'states': Tensor (T, state_dim),
            'actions': Tensor (T, action_dim),
            'rewards': Tensor (T, 1),
        }
        """
    
    def sample_batch(self, batch_size: int, 
                     seq_len: int = 50) -> Dict[str, torch.Tensor]:
        """
        采样一个 batch 的序列片段
        返回 {
            'states': (batch, seq_len, state_dim),
            'actions': (batch, seq_len, action_dim),
            'rewards': (batch, seq_len, 1),
        }
        """
    
    def __len__(self) -> int:
        """总步数"""
```

#### 验收标准 E

| 编号 | 测试项 | 通过标准 | 验证方法 |
|------|--------|---------|---------|
| E1 | 存取正确 | 存入取出后数据一致 | `test_store_retrieve()` |
| E2 | 容量限制 | 超过 capacity 后自动丢弃最旧数据 | `test_capacity()` |
| E3 | 采样均匀 | 各 episode 被采样频率大致相等 | χ² 检验 |
| E4 | 序列完整 | 采样的序列片段来自同一 episode | `test_sequence_integrity()` |

---

### 2.6 模块 F：跨维度迁移（`experiments/transfer.py`）

#### 迁移策略

由于不同维度的环境，状态和动作的维度不同（4D 环境状态是 20 维，3D 环境状态是 15 维），需要处理维度不匹配。

```
迁移策略 1：只迁移隐藏层（推荐）

4D 模型：                     3D 模型：
  Input(20) → Linear          Input(15) → Linear      ← 不迁移（维度不同）
  → Hidden(256) → Hidden(256) → Hidden(256) → Hidden(256)  ← 迁移这些层
  → Output(20)                → Output(15)             ← 不迁移（维度不同）

迁移策略 2：填充/截断输入层

4D 模型的输入层权重：W ∈ R^{256×20}
3D 模型需要的输入层：W ∈ R^{256×15}
  → 取 W 的前 15 列

迁移策略 3：适配器（Adapter）

在预训练的隐藏层前后加小的适配层：
  Input(15) → Adapter(15→256) → [冻结的预训练隐藏层] → Adapter(256→15)
```

```python
class DimensionTransfer:
    """跨维度迁移工具"""
    
    def __init__(self, source_dim: int, target_dim: int,
                 transfer_strategy: str = 'hidden_only'):
        self.source_dim = source_dim
        self.target_dim = target_dim
        self.strategy = transfer_strategy
    
    def transfer(self, source_model: BaseWorldModel, 
                 target_model: BaseWorldModel) -> BaseWorldModel:
        """
        把 source_model 的知识迁移到 target_model
        返回初始化好的 target_model
        """
        source_state = source_model.state_dict()
        target_state = target_model.state_dict()
        
        transferred = 0
        skipped = 0
        
        for key in target_state:
            if key in source_state:
                if target_state[key].shape == source_state[key].shape:
                    # 形状匹配：直接复制
                    target_state[key] = source_state[key].clone()
                    transferred += 1
                else:
                    if self.strategy == 'truncate':
                        # 截断/填充策略
                        target_state[key] = self._truncate_or_pad(
                            source_state[key], target_state[key].shape
                        )
                        transferred += 1
                    else:
                        skipped += 1
            else:
                skipped += 1
        
        target_model.load_state_dict(target_state)
        print(f"迁移完成: {transferred} 层迁移, {skipped} 层跳过")
        return target_model
    
    def _truncate_or_pad(self, tensor, target_shape):
        """截断或零填充张量到目标形状"""
        result = torch.zeros(target_shape)
        slices = tuple(slice(0, min(s, t)) 
                      for s, t in zip(tensor.shape, target_shape))
        result[slices] = tensor[slices]
        return result
```

#### 验收标准 F

| 编号 | 测试项 | 通过标准 | 验证方法 |
|------|--------|---------|---------|
| F1 | 迁移后模型能运行 | 迁移后前向传播无报错 | `test_transfer_forward()` |
| F2 | 迁移层权重正确 | 迁移的层和源模型完全一致 | `test_weight_identity()` |
| F3 | 未迁移层随机初始化 | 维度不匹配的层不是全零 | `test_random_init()` |
| F4 | 迁移后可训练 | 迁移后继续训练 10 epoch 无错 | `test_finetune()` |

---

### 2.7 模块 G：实验协议（`experiments/`）

#### 实验 1：基准性能（各维度独立训练）

```
目的：建立各维度的性能基线
设置：
  维度 = [2, 3, 4, 5, 6, 8]
  模型 = GRU
  种子 = [0, 1, 2, 3, 4]（5 个种子取平均和标准差）
  训练 epoch = 100
  
记录：
  - 每 epoch 的真实环境奖励
  - 世界模型预测误差
  - 任务成功率
  - 训练时间
```

#### 实验 2：核心迁移实验（N维 → 3D）

```
目的：验证假设 H1 和 H2
设置：
  源维度 = [2, 3, 4, 5, 6, 8]
  目标维度 = 3
  预训练 epoch = 50
  微调 epoch = 50
  种子 = [0, 1, 2, 3, 4]
  迁移策略 = hidden_only

基线：3D 从头训练 100 epoch
  
记录：
  - 迁移后第 1, 5, 10, 20, 50 epoch 的性能
  - 与基线对比的学习加速比
  - 最终性能对比
```

#### 实验 3：鲁棒性测试

```
目的：验证假设 H3
设置：
  在训练好的模型上测试以下扰动：
  - 物体质量变化 ±50%
  - 重力变化 ±30%
  - 添加随机外力噪声
  - 初始条件分布偏移

记录：
  - 各扰动条件下的成功率
  - 4D 预训练 vs 3D 直接训练的鲁棒性对比
```

#### 实验 4：消融实验

```
目的：理解什么因素驱动了迁移效果
  
消融项：
  a) 模型架构（MLP vs GRU vs RSSM）
  b) 预训练时长（10 vs 30 vs 50 vs 100 epoch）
  c) 迁移策略（hidden_only vs truncate vs adapter）
  d) 想象步长（H = 5 vs 15 vs 30）
  e) 任务难度（easy vs medium vs hard）
```

#### 验收标准 G

| 编号 | 测试项 | 通过标准 | 验证方法 |
|------|--------|---------|---------|
| G1 | 实验可复现 | 相同种子 ± 配置产生相同结果 | 两次独立运行对比 |
| G2 | 统计显著性 | 5 个种子的标准差 < 均值的 30% | 误差棒检查 |
| G3 | 基线合理 | 3D 直接训练能学会任务 | 成功率 > 50% |
| G4 | 结果可解释 | 维度扫描曲线有明确趋势 | 人工检查曲线形状 |
| G5 | 计算预算可控 | 全部实验在 Colab Pro 上 < 48 小时 | 计时 |

---

### 2.8 模块 H：评估与可视化（`experiments/visualize.py`）

#### 输出图表清单

```
图 1：维度扫描曲线（核心图）
  X 轴：预训练维度 (2, 3, 4, 5, 6, 8)
  Y 轴：3D 任务最终得分
  包含误差棒
  标注 3D 直接训练的基线

图 2：学习曲线对比
  多条线：各维度预训练 → 3D 微调的学习曲线
  基线：3D 从头训练
  X 轴：微调 epoch
  Y 轴：3D 任务奖励

图 3：世界模型预测精度
  X 轴：rollout 步数 (1-30)
  Y 轴：预测误差
  多条线：各维度预训练的模型

图 4：鲁棒性热力图
  X 轴：扰动类型
  Y 轴：预训练维度
  颜色：成功率

图 5：消融实验条形图
  各消融项的性能对比

图 6：t-SNE 表征可视化
  4D 模型 vs 3D 模型的潜空间表征
  颜色编码物理属性（速度、距离等）
```

#### 验收标准 H

| 编号 | 测试项 | 通过标准 | 验证方法 |
|------|--------|---------|---------|
| H1 | 图表可自动生成 | 一键运行脚本生成所有图 | `python visualize.py` |
| H2 | 图表清晰可读 | 字体 ≥ 12pt，有图例，有轴标签 | 人工检查 |
| H3 | 数据有误差棒 | 所有曲线包含 5 种子的均值 ± 标准差 | 人工检查 |
| H4 | 结果可复现 | 从检查点重新生成图表与原始一致 | 对比检查 |

---

## 3. 里程碑与时间表

```
里程碑 1（第 2 周末）：物理引擎 + 环境就绪
  ├── A1-A7 全部通过
  ├── B1-B7 全部通过
  └── 可以用随机动作在 2D-8D 环境中运行

里程碑 2（第 4 周末）：世界模型训练跑通
  ├── C1-C7 全部通过
  ├── D1-D7 全部通过
  ├── E1-E4 全部通过
  └── 在 4D 环境中，世界模型能合理预测下一步

里程碑 3（第 6 周末）：核心实验完成
  ├── F1-F4 全部通过
  ├── 实验 1（基准性能）完成
  ├── 实验 2（迁移实验）完成
  └── 维度扫描曲线已画出

里程碑 4（第 8 周末）：全部完成
  ├── G1-G5 全部通过
  ├── H1-H4 全部通过
  ├── 实验 3（鲁棒性）完成
  ├── 实验 4（消融）完成
  ├── 全部图表生成
  └── 技术报告初稿完成
```

---

## 4. 风险清单与应对

| 风险 | 概率 | 影响 | 应对措施 |
|------|------|------|---------|
| 4D→3D 迁移效果不显著 | 中 | 高 | 这本身就是一个有价值的发现（负结果也是结果）；调整迁移策略；增加预训练时长 |
| 世界模型多步预测误差爆炸 | 中 | 中 | 缩短想象步长 H；加正则化；试 RSSM（概率模型更稳定） |
| Colab 断连丢失进度 | 高 | 低 | 每 30 分钟自动保存检查点到 Google Drive |
| 4D 物理引擎有 bug | 中 | 高 | 严格的单元测试（能量守恒、动量守恒）；先在 2D/3D 验证（可以可视化） |
| 训练不收敛 | 低 | 高 | 先在最简单的设置（2D+MLP+easy）验证；逐步增加复杂度 |
| 显存不够 | 低 | 中 | 减小 batch_size 和 hidden_dim；状态向量输入本身就很省显存 |

---

## 5. 成功标准总表

### 最低成功标准（必须达到）

| 项目 | 标准 |
|------|------|
| 物理引擎在 2D-8D 运行正确 | A1-A7 全部通过 |
| 至少一个世界模型架构能学会预测 | C3 通过（单步 MSE < 0.1） |
| 策略在至少一个维度上能学会任务 | 成功率 > 50% |
| 维度扫描曲线已画出 | 至少 4 个维度的数据点 |
| 迁移实验有结果（无论正负） | 4D→3D 实验完成 |

### 理想成功标准（争取达到）

| 项目 | 标准 |
|------|------|
| 4D 预训练确实加速 3D 学习 | 学习速度提升 > 30% |
| 维度扫描曲线呈现有意义的趋势 | 存在 sweet spot 或单调趋势 |
| 鲁棒性实验显示高维预训练更鲁棒 | 扰动下性能下降 < 基线的 50% |
| RSSM 比 GRU 效果更好 | 消融实验确认 |
| 技术报告达到 workshop 投稿水平 | 结构完整、实验充分、分析深入 |

### 超额成功标准（如果达到就是惊喜）

| 项目 | 标准 |
|------|------|
| 零样本迁移（4D→3D 不微调）有效 | 成功率 > 20% |
| 发现一个明确的"最优预训练维度" | 维度扫描曲线有清晰峰值 |
| 表征分析揭示了有意义的结构 | t-SNE 显示物理属性聚类 |

---

## 6. 文件/目录结构（最终版）

```
HyperDream/
│
├── README.md                          # 项目概述
├── requirements.txt                   # torch, numpy, matplotlib, gymnasium, tensorboard
├── setup.sh                           # Colab 一键安装脚本
│
├── configs/
│   ├── physics.yaml                   # 物理引擎参数
│   ├── env.yaml                       # 环境参数
│   ├── model.yaml                     # 模型超参数
│   └── experiment.yaml                # 实验配置
│
├── physics/
│   ├── __init__.py
│   ├── body.py                        # Body4D 类
│   ├── engine.py                      # Physics4D 引擎
│   └── tests/
│       ├── test_conservation.py       # A2-A3
│       ├── test_free_fall.py          # A4
│       └── test_determinism.py        # A7
│
├── envs/
│   ├── __init__.py
│   ├── push_ball.py                   # PushBallNDEnv
│   ├── navigation.py                  # （可选）导航任务
│   └── tests/
│       ├── test_env_basics.py         # B1-B3
│       └── test_difficulty.py         # B4-B5
│
├── models/
│   ├── __init__.py
│   ├── base.py                        # BaseWorldModel 接口
│   ├── mlp_world_model.py             # Level 1
│   ├── gru_world_model.py             # Level 2
│   ├── rssm_world_model.py            # Level 3
│   ├── policy.py                      # 策略网络
│   └── tests/
│       ├── test_forward.py            # C1-C2
│       └── test_prediction.py         # C3-C6
│
├── training/
│   ├── __init__.py
│   ├── buffer.py                      # ReplayBuffer
│   ├── dream_trainer.py               # DreamTrainer 主循环
│   ├── transfer.py                    # DimensionTransfer
│   └── tests/
│       ├── test_buffer.py             # E1-E4
│       └── test_transfer.py           # F1-F4
│
├── experiments/
│   ├── run_baseline.py                # 实验 1
│   ├── run_transfer.py                # 实验 2
│   ├── run_robustness.py              # 实验 3
│   ├── run_ablation.py                # 实验 4
│   └── visualize.py                   # 生成所有图表
│
├── notebooks/
│   ├── 01_physics_demo.ipynb          # 物理引擎交互演示
│   ├── 02_training_demo.ipynb         # 训练过程演示
│   ├── 03_transfer_demo.ipynb         # 迁移实验演示
│   └── 04_full_experiment.ipynb       # 完整实验（Colab 用）
│
├── checkpoints/                       # 模型存档
├── results/                           # 实验结果数据
├── figures/                           # 生成的图表
└── report/
    └── technical_report.md            # 最终技术报告
```