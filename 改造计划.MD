# 🚀 High_Dimensional_WorldModel 改造计划（Upgrade Plan）

## 0) 文档定位（Document Scope）

```text
[目标]
├─ 让当前 4D/5D 仿真具备“可迁移、可升级、可验证”能力
├─ 避免未来切换高保真引擎时推倒重来
└─ 建立分阶段验收标准（Acceptance Criteria）
```

- 版本：`v1.0`
- 日期：`2026-02-27`
- 适用范围：当前仓库全部核心模块（`physics/`, `envs/`, `models/`, `training/`, `experiments/`）

---

## 1) 改造总图（Roadmap Map）

```text
[P0 基线冻结 Baseline Freeze]
        |
        v
[P1 接口稳定 Semantic Interface Lock]
        |
        v
[P2 参数辨识 + 域随机化 SysID + Domain Randomization]
        |
        v
[P3 物理主干 + 残差学习 Physics+Residual]
        |
        v
[P4 课程学习 + 鲁棒性 Curriculum + Robustness]
        |
        v
[P5 准HPC化 Orchestration + Scale]
        |
        v
[P6 高保真迁移 Hi-Fi Migration]
```

---

## 2) 全局验收口径（Global Definition of Done）

```text
[DoD]
├─ 可复现 (Reproducible)
│  └─ 固定seed可复现主要指标（允许小波动）
├─ 可迁移 (Transferable)
│  └─ 升级后旧策略/旧模型不完全失效
├─ 可解释 (Explainable)
│  └─ 指标变化可定位到模块与参数
└─ 可回滚 (Rollback-ready)
   └─ 每阶段都有退路与兼容开关
```

统一指标（核心KPI）：
- `K1`：`baseline_success@3D_easy`
- `K2`：`transfer_gain = transfer_success - scratch_success`
- `K3`：`robustness_success@medium/hard`
- `K4`：`wm_one_step_mse`（world model one-step error）
- `K5`：训练吞吐（episodes/hour 或 steps/sec）

---

## 3) 分阶段细化（Phased Plan + Acceptance）

## P0) 基线冻结（Baseline Freeze）

```text
[P0]
├─ 目标: 固化“当前能跑通”的事实，作为未来对照组
├─ 风险等级: M (Medium)
└─ 时长建议: 1-2 天
```

工作项（Work Items）：
1. 固化当前最佳参数集（baseline/transfer/robustness）。
2. 固化对照结果到 `results/`，并标记 run-id。
3. 建立 `3 seeds` 基准统计（均值/标准差）。

验收标准（Acceptance Criteria）：
1. `3 seeds` 的 `3D easy` 成功率标准差 `std <= 0.08`。
2. `results/*.json` 与 `results/<experiment>/<run_id>/` 完整可追溯。
3. 复跑一次同命令，关键指标偏差在 `±10%` 内。

建议命令（可复现）：
```bash
python experiments/run_baseline.py --run-id p0_baseline_seed1 --epochs 8 --eval-episodes 40
python experiments/run_transfer.py --run-id p0_transfer_seed1 --pretrain-epochs 6 --finetune-epochs 6 --eval-episodes 40
python experiments/run_robustness.py --run-id p0_robust_seed1 --episodes 120 --dim 3
```

---

## P1) 接口稳定（Semantic Interface Lock）

```text
[P1]
├─ 目标: 锁定“状态语义”与“动作语义”，后续升级不改接口
├─ 风险等级: M
└─ 时长建议: 2-4 天
```

工作项：
1. 明确并文档化状态语义：  
   `state = [agent_pos, agent_vel, ball_pos, ball_vel, target_pos]`
2. 在 `env` 与 `trainer` 增加接口契约检查（shape + semantic assertions）。
3. 新增接口回归测试（旧checkpoint能加载、旧policy能推理）。

验收标准：
1. `state_dim == 5*dim`、`action_dim == dim` 在 2/3/4/5/6/8D 全通过。
2. 历史 checkpoint 兼容加载通过率 `100%`（同模型类型）。
3. 接口回归测试全部通过。

建议命令：
```bash
pytest -q envs/tests/test_env_basics.py models/tests/test_forward.py training/tests/test_dream_trainer.py
```

---

## P2) 参数辨识 + 域随机化（SysID + Domain Randomization）

```text
[P2]
├─ 目标: 降低 sim gap（仿真-现实差距）
├─ 风险等级: H (High)
└─ 时长建议: 1-2 周
```

工作项：
1. 引入随机参数分布（重力、阻尼、风、质量、接触增益）。
2. 配置化参数采样（每回合/每批次采样）。
3. 训练时采用分布期望目标：
   `max E_{theta~p(theta)}[R(pi)]`。

验收标准：
1. 在参数扰动集上，`3D easy` 成功率下降不超过 `20%`。
2. `medium/hard` 成功率相对 P0 提升（至少 `+0.08` 绝对值）。
3. 新增随机化配置后，训练过程无数值爆炸（NaN/Inf 为 0 次）。

建议命令：
```bash
python experiments/run_robustness.py --run-id p2_rand_robust --episodes 200 --dim 3
pytest -q
```

---

## P3) 物理主干 + 残差学习（Physics + Residual）

```text
[P3]
├─ 目标: 用“可解释物理”打底，用“数据驱动残差”补误差
├─ 风险等级: H
└─ 时长建议: 2-3 周
```

核心形式（关键公式）：
\[
s_{t+1}=f_{phys}(s_t,a_t;\theta)+r_{\phi}(s_t,a_t)
\]

工作项：
1. 在 world model 侧新增 residual head（输出状态残差、奖励残差）。
2. 增加 ablation：`pure_phys` vs `pure_nn` vs `phys+residual`。
3. 建立 one-step 与 multi-step 误差曲线报告。

验收标准：
1. `wm_one_step_mse` 相比 P0 降低 `>= 20%`。
2. `rollout@20 steps` 的累计误差相比 P0 降低 `>= 15%`。
3. `phys+residual` 在 transfer 任务中不弱于 `pure_nn`（至少持平）。

建议命令：
```bash
python experiments/run_ablation.py --run-id p3_ablation --epochs 10 --eval-episodes 50
pytest -q models/tests/test_prediction.py training/tests/test_dream_trainer.py
```

---

## P4) 课程学习 + 鲁棒性提升（Curriculum + Robustness）

```text
[P4]
├─ 目标: 先学会，再学稳，再学难
├─ 风险等级: M
└─ 时长建议: 1-2 周
```

工作项：
1. 难度课程（easy -> medium -> hard）自动调度。
2. 失败案例回放加权（hard negative replay）。
3. 对 medium/hard 单独做阈值驱动训练（until-success）。

验收标准：
1. `robustness@medium >= 0.35`
2. `robustness@hard >= 0.25`
3. 课程学习后，`easy` 不显著退化（下降不超过 `0.05` 绝对值）。

建议命令：
```bash
python experiments/run_until_success.py --run-id p4_curriculum --dim 3 --difficulty medium --target-success 0.35 --max-rounds 30
python experiments/run_until_success.py --run-id p4_curriculum --dim 3 --difficulty hard --target-success 0.25 --max-rounds 40
```

---

## P5) 准HPC化（Scale-out Orchestration）

```text
[P5]
├─ 目标: 从单机/Colab 走向批处理与并行实验
├─ 风险等级: M
└─ 时长建议: 1-2 周
```

工作项：
1. 完善批量任务编排（Kaggle/本地并发/后续可迁移SLURM）。
2. 多seed自动聚合报表（均值、方差、置信区间）。
3. 加入阶段心跳与失败重试策略。

验收标准：
1. 一键发起并完成 `>= 12` 个任务（多维度+多seed）成功率 `>= 90%`。
2. 自动汇总报告可直接出图（维度扫描、迁移矩阵、鲁棒性条形图）。
3. 失败任务可自动重试并记录 root cause。

建议命令：
```bash
python kaggle_job_manager.py run --owner <your_owner> --slug high-dimensional-worldmodel-aggressive
```

---

## P6) 高保真迁移（Hi-Fi Migration）

```text
[P6]
├─ 目标: 接入更高保真引擎/数据源，验证可迁移性
├─ 风险等级: H
└─ 时长建议: 2-6 周
```

工作项：
1. 对接高保真数据（或外部仿真器）并做参数对齐。
2. 只替换 `f_phys`，保持状态/动作语义不变。
3. 残差模型小步微调，不从零重训。

验收标准：
1. 升级后 `3D easy` 保底性能不低于 P4 的 `90%`。
2. 升级后 `medium/hard` 有可观提升（至少一项 +0.10）。
3. 迁移耗时显著低于从零训练（训练步数降低 `>= 30%`）。

---

## 4) 阶段门禁与测试矩阵（Risk-based Validation Matrix）

```text
[Risk Tier]
├─ L: 文档/小改动
│  └─ Smoke + 格式/一致性校验
├─ M: 常规功能改动
│  └─ Unit + Integration + 关键路径回归
└─ H: 架构/核心训练改动
   └─ Full critical suite + 多seed + 回滚演练
```

每阶段输出必须包含：
1. `Risk Tier`
2. 已执行验证动作与结果（Pass/Fail）
3. 覆盖范围（Coverage）
4. 未覆盖风险（Residual Risk）
5. 若受阻，给出阻塞原因与补测命令

---

## 5) 依赖关系图（Dependency Map）

```text
P0 --> P1 --> P2 --> P3 --> P4 --> P6
                  \
                   --> P5 ----/
```

说明：
- `P5`（规模化）可以和 `P3/P4` 并行推进。
- `P6` 依赖 `P1`（接口稳定）和 `P3`（残差能力）基本完成。

---

## 6) 回滚策略（Rollback Plan）

```text
[Rollback]
├─ 配置回滚: 切回上一个稳定 config
├─ 模型回滚: 使用 best checkpoint
├─ 结果回滚: 保留 run_id 分层目录，禁止覆盖
└─ 发布回滚: 阶段开关默认可关闭
```

最低回滚要求：
1. 每阶段至少保留 `latest + best + periodic` 检查点。
2. 关键参数改动必须可通过 config 切换关闭。

---

## 7) 当周执行建议（Next 7 Days）

```text
[Week-1 Sprint]
├─ Day1-2: P0 基线冻结（3 seeds）
├─ Day3-4: P1 接口契约测试补全
├─ Day5-7: P2 域随机化最小版本上线
└─ 里程碑: 给出第一版“抗sim gap”报告
```

周验收标准：
1. 输出 `baseline/transfer/robustness` 的 `3-seed` 汇总表。
2. 输出域随机化前后对比（至少 easy + medium 两档）。
3. 全测试通过，且新增测试覆盖新逻辑。

